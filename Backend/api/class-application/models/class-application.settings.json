{
  "kind": "collectionType",
  "collectionName": "class_applications",
  "info": {
    "name": "classApplication",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "tutor": {
      "via": "class_applications",
      "model": "tutor"
    },
    "request": {
      "model": "request"
    },
    "note": {
      "type": "text"
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "pending",
        "approved"
      ],
      "default": "pending"
    }
  }
}
