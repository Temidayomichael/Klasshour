{
  "routes": [
    {
      "method": "GET",
      "path": "/wallet-topups",
      "handler": "wallet-topup.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/wallet-topups/count",
      "handler": "wallet-topup.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/wallet-topups/:id",
      "handler": "wallet-topup.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/wallet-topups",
      "handler": "wallet-topup.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/wallet-topups/:id",
      "handler": "wallet-topup.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/wallet-topups/:id",
      "handler": "wallet-topup.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
